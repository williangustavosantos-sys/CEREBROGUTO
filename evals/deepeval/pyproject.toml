[project]
name = "guto-v3-deepeval"
version = "0.1.0"
description = "Multi-turn conversational regressions for GUTO Cerebro V3.1"
requires-python = ">=3.12,<3.14"
dependencies = [
  "deepeval>=3.9.0,<4.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
